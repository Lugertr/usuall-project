export interface StoreInfo {
  shop: number;
  insales_id: string;
  user_email: string;
  user_id: number;
}

export interface User {
    username: string,
    password: string
}

export type JWT = string
